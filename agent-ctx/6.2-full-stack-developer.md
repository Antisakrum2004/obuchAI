---
Task ID: 6.2
Agent: full-stack-developer
Task: Sprint 6 API implementation

Work Log:
- Read existing codebase: articles route, articles/[id] route, db.ts, auth.ts, schema.prisma, migrate route, glossary route
- Installed jszip package via npm
- Created /src/lib/gen-id.ts — shared ID generator function with optional prefix
- Created /src/app/api/knowledge/import/route.ts — ZIP Import endpoint (POST, admin-only, JSZip extraction, folder→article+queue mapping)
- Created /src/app/api/knowledge/process/route.ts — AI Processing trigger (POST, admin-only, creates queue entries per type)
- Created /src/app/api/knowledge/process/[id]/route.ts — Queue item CRUD (GET status, PUT update progress/result, DELETE cancel)
- Created /src/app/api/knowledge/queue/route.ts — Queue management (GET list with status/type/articleId filters, admin-only)
- Created /src/app/api/knowledge/ai/route.ts — AI Processing worker (POST, admin-only, uses z-ai-web-dev-sdk for metadata/glossary/graph processing)
- Modified /src/app/api/knowledge/articles/route.ts — Added videoUrl, pdfUrl, pptxUrl, sourceUrl, sourceType, difficulty, estimatedTime, status, aiGenerated fields to POST INSERT
- Modified /src/app/api/knowledge/articles/[id]/route.ts — Added new fields to SELECT query, result object, and allowedFields/jsonFields arrays in PUT
- Fixed lint warning in import route (replaced while(true) with while(slugExists))
- Verified: no TypeScript errors in new/modified files, no ESLint errors in new files

Stage Summary:
- Created 6 new files and modified 2 existing files
- All new API routes follow existing code patterns (pool.query, getServerSession, admin check, Russian error messages)
- AI processing endpoint implements 3 types: metadata (summary/difficulty/tags), glossary (term extraction), graph (prerequisites/nextTopics)
- Articles POST now supports 9 new Sprint 6 fields
- Articles [id] GET now returns 12 new fields, PUT allows updating 14 new fields
- All code uses raw SQL via pool.query (not Prisma)
